---
import Link from "../ui/link/Link.astro";
import SocialLinks from "../ui/SocialLinks.astro";
import {
    footer as footerLinkSections,
    SITE_SETTINGS as site,
} from "../../site.config";

const currentYear = new Date().getFullYear();
---

<footer
    class="relative z-50 mt-24 border-t border-[var(--surface-border)] bg-[var(--surface-muted)]"
>
    <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
        <div class="py-16">
            <div
                class="flex flex-wrap gap-x-12 gap-y-10 sm:flex-row sm:items-start sm:justify-between lg:flex-nowrap"
            >
                <!-- Brand -->
                <div class="order-1 max-w-2xs space-y-4 lg:order-1">
                    <h2 class="text-lg font-semibold">
                        {site.title}
                    </h2>
                    <p class="text-sm text-[var(--text-muted)]">
                        {site.description}
                    </p>

                    <SocialLinks />
                </div>

                <!-- Footer Link Sections -->
                {
                    footerLinkSections.map((section) => (
                        <div class="order-2 flex-1 basis-0 lg:order-2 lg:flex-none lg:basis-auto">
                            <h3 class="text-sm font-semibold tracking-wider uppercase">
                                {section.title}
                            </h3>
                            <ul class="mt-4 space-y-2">
                                {section.links.map((link) => (
                                    <li>
                                        <Link
                                            variant="plain"
                                            href={link.url}
                                            class="text-sm"
                                        >
                                            {link.name}
                                        </Link>
                                    </li>
                                ))}
                            </ul>
                        </div>
                    ))
                }
            </div>
        </div>

        <!-- Bottom Bar -->
        <div class="border-t border-[var(--surface-border)] py-8">
            <div
                class="flex flex-col items-center justify-between gap-4 sm:flex-row"
            >
                <p class="text-sm">
                    &copy; {currentYear}
                    {site.title}. All rights reserved.
                </p>
                <div class="flex space-x-6">
                    <Link
                        variant="plain"
                        href="/legal/privacy-policy"
                        class="text-sm"
                    >
                        Privacy Policy
                    </Link>
                    <Link
                        variant="plain"
                        href="/legal/terms-of-service"
                        class="text-sm"
                    >
                        Terms of Service
                    </Link>
                </div>
            </div>
        </div>
    </div>
</footer>
